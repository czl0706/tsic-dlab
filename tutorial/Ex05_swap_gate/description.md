Build a 2-bit swap circuit.

Use dataflow logic to build a 2-bit comparator and gate-level logic to build two 2-bit muxes. The top module should output the smaller value on `lo` and the larger value on `hi`.
