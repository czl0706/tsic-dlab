Implement a small ALU component with four registers.

Support register writes through `wen`, `waddr`, and `wdata`. Select operands with `src_a` and `src_b`, then support add, subtract, multiply, and divide-by-2 operations.